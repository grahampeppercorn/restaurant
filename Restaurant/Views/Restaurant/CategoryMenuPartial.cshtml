@model IEnumerable<Restaurant.Models.ViewModels.Restaurant.CategoryVM>

<h3>Categories</h3>

<ul>
    @foreach (var item in Model)
    {
        <li>
            <a href="/restaurant/category/@item.Url.ToLower()">@item.Name</a>
        </li>
    }
</ul>